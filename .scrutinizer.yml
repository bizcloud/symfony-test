build:
  environment:
    php:
      version: 7.4
  tests:
    override:
      -
        command: /var/www/symfony-test/bin/phpunit --coverage-clover=coverage.xml
        coverage:
          file: coverage.xml
          format: php-clover
